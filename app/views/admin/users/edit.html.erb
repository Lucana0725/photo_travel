<div class="container">
  <div class="row">
    <h1>
      <%= @user.nickname %> さんの会員情報編集画面
    </h1>
  </div>
  
  <div class="row">
    <div>
      この画面では登録ユーザーの退会情報の編集ができます。
      不適切なユーザーは管理者権限で強制退会が可能です。
    </div>
  </div>
    
  <div class="row">
    <%= form_with model: @user, url: admin_user_path(@user), method: :patch do |f| %>
      <table class="table table-borderless">
        <tr>
          <th>ユーザーID</th>
          <td><%= @user.id %></td>
        </tr>
        <tr>
          <th>ニックネーム</th>
          <td><%= @user.nickname %></td>
        </tr>
        <tr>
          <th>登録名（姓）</th>
          <td><%= @user.last_name %></td>
          <th>（名）</th>
          <td><%= @user.first_name %></td>
        </tr>
        <tr>
          <th>登録名（セイ）</th>
          <td><%= @user.last_name_kana %></td>
          <th>（メイ）</th>
          <td><%= @user.first_name_kana %></td>
        </tr>
        <tr>
          <th>メールアドレス</th>
          <td><%= @user.email %></td>
        </tr>
        <tr>
          <th>退会フラグ</th>
          <td>
            <%= f.radio_button :is_deleted, "false" %>
            <%= f.label :is_deleted_false, "有効" %>
            
            <%= f.radio_button :is_deleted, "true" %>
            <%= f.label :is_deleted_true, "退会" %>
          </td>
        </tr>
      </table>
      <%= f.submit "更新する", class: "btn btn-success" %>
    <% end %>
  </div>
</div>
